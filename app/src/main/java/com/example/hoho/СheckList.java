package com.example.hoho;

public class СheckList {
    /*кастомный адаптор:
        сделать нормально кнопки

     */

    /*для себя
    Это код который позволяет задовать размеры програмно
    ImageView imageView;
        int dp1 = (int) TypedValue.applyDimension(TypedValue.COMPLEX_UNIT_DIP, 1,
                context.getResources().getDisplayMetrics());

        imageView.setLayoutParams(new ViewGroup.LayoutParams(
                ViewGroup.LayoutParams.WRAP_CONTENT,
                dp1 * 100)); // if you want to set layout height to 100dp

 */
    /*
    // создаем адаптер
        ImageAdapter adapter = new ImageAdapter(this, isList1());
        ImageAdapter adapter2 = new ImageAdapter(this, isList2());
        gridView1.setAdapter(adapter);
        gridView1.setAdapter(adapter);

       //обработка клика
       adapter.setOnItemClickListener(position -> {
            Intent intent = new Intent(MainActivity.this, MainActivity2.class);
            startActivity(intent);
        });
        //устанавливаем адаптер
        gridView1.setAdapter(adapter);
     */
}
